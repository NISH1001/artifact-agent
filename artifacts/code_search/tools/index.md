# Tools

Available tools for repository discovery and URL handling:

- **github_search/** — Search GitHub repositories via REST API
- **gitlab_search/** — Search GitLab repositories via REST API
- **parse_github_url/** — Parse a GitHub URL into owner/repo (pure compute, no I/O)
