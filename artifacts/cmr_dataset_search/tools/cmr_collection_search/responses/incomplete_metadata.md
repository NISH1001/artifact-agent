# incomplete_metadata

## When this happens

One or more key UMM fields (Abstract, TemporalExtents, SpatialExtent, ProcessingLevelId) are null or empty

## Agent hint

Metadata is incomplete for this collection. Flag missing fields. Do not discard the collection — rank it lower and note gaps in the output. Key fields to check: Abstract, TemporalExtents, SpatialExtent, ProcessingLevelId, ScienceKeywords.

## User message

This dataset was found but has incomplete metadata (missing: {field_list}). Review directly on Earthdata Search before use.

## Suggested next action

Include collection in results with metadata completeness flag; surface gaps to user
