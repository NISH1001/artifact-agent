# success

## When this happens

Paper search returns 1 or more results with non-empty abstracts

## Agent hint

Papers retrieved. Extract from each abstract: (1) variable names mentioned, (2) dataset short names or instrument names cited, (3) spatial/temporal resolution descriptions, (4) processing level mentions. Add any new variables to the working variable list before re-querying CMR.

## User message

Found {count} relevant papers. Using them to expand the variable list for dataset search.

## Suggested next action

Extract variables and dataset mentions; merge into variable list; re-run cmr_collection_search if new variables found
